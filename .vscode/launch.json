{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug FASTA parser",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/scripts/fasta_summary.py",
            "args": ["data/sequences.fasta"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}